/** Set to false before public launch to remove the staging password gate. */
export const STAGING_GATE_ENABLED = true;

export const STAGING_PASSWORD = "nciadmin";
export const STAGING_STORAGE_KEY = "nci-staging-unlock";
